events {
    worker_connections 1024;
}

http {
    upstream weatherops_backend {
        server weatherops:8000;
    }

    server {
        listen 80;
        server_name localhost;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-Content-Type-Options "nosniff";
        add_header X-XSS-Protection "1; mode=block";

        # Proxy to FastAPI
        location / {
            proxy_pass http://weatherops_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_connect_timeout 30s;
            proxy_read_timeout 60s;
        }

        # Static files
        location /static/ {
            proxy_pass http://weatherops_backend/static/;
        }

        # Health check endpoint
        location /health {
            proxy_pass http://weatherops_backend/health;
            access_log off;
        }
    }
<<<<<<< HEAD
}
=======
}
>>>>>>> 04545d62421a0742ac53ed111b5072899c582a02
